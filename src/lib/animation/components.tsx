import { Group, Stack, Container, SimpleGrid, Progress } from '@mantine/core'
import { motion } from 'framer-motion'

export const MotionGroup = motion(Group)
export const MotionStack = motion(Stack)
export const MotionContainer = motion(Container)
export const MotionSimpleGrid = motion(SimpleGrid)

export const MotionProgress = motion(Progress)
